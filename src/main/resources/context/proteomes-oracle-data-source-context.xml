<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa" xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                            http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/data/jpa http://www.springframework.org/schema/data/jpa/spring-jpa.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <context:annotation-config/>

    <jpa:repositories base-package="uk.ac.ebi.pride.proteomes.db.core.api"/>

    <!--Setup data source using place holder properties-->
    <bean id="proteomesDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource" destroy-method="close">
        <property name="driverClass" value="${pride.prot.jdbc.driver}"/>
        <property name="jdbcUrl" value="${pride.prot.jdbc.url}"/>
        <property name="user" value="${pride.prot.jdbc.user}"/>
        <property name="password" value="${pride.prot.jdbc.password}"/>
        <!--<property name="validationQuery" value="select 1 from dual"/>-->
        <!--<property name="testWhileIdle" value="true"/>-->
    </bean>


    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!--pride-loader beans -->
    <bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="dataSource" ref="proteomesDataSource"/>
        <property name="persistenceUnitName" value="pride-proteomes-db-core"/>
        <property name="jpaVendorAdapter">
            <bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
                <property name="database" value="ORACLE"/>
                <property name="showSql" value="false"/>
                <!--to enable details SQL output for debugging-->
            </bean>
        </property>
        <property name="jpaProperties"> <!-- needed for lazy loading, otherwise the session is not kept even if transactional: http://stackoverflow.com/questions/578433/how-to-solve-lazy-initialization-exception-using-jpa-and-hibernate-as-provider -->
            <props>
                <!-- However in Hibernate 4.1.8 and 4.1.9 there is a bug related to this feature https://hibernate.atlassian.net/browse/HHH-7971
                     hence we have to use 4.1.7 !
                -->
                <prop key="hibernate.enable_lazy_load_no_trans">true</prop>
            </props>
        </property>
    </bean>

    <bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
        <property name="entityManagerFactory" ref="entityManagerFactory"/>
        <property name="jpaDialect">
            <bean class="org.springframework.orm.jpa.vendor.HibernateJpaDialect"/>
        </property>
    </bean>

    <bean class="org.springframework.orm.hibernate4.HibernateExceptionTranslator"/>

    <!--load property files-->
    <!-- Set up or detect a System property called "DB_ENVIRONMENT" used to construct a properties file on the classpath.  The default is "test".  -->
    <!-- See scripts to have a look how to use this environment -->
    <bean id="dbEnvironment"
          class="org.springframework.batch.support.SystemPropertyInitializer">
        <property name="defaultValue" value="test"/>
        <property name="keyName" value="DB_ENVIRONMENT"/>
    </bean>

    <bean id="dbPlaceholderProperties" class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer" depends-on="dbEnvironment">
        <property name="location" value="classpath:prop/db/pride-proteomes-db-${DB_ENVIRONMENT}.properties"/>
        <property name="ignoreUnresolvablePlaceholders" value="true"/>
        <property name="order" value="1"/>
    </bean>
</beans>
